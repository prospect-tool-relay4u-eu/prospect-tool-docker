# Copy to .env and adjust if you like — these are placeholder sandbox credentials,
# not secrets, and only ever exist in your own local containers.
DB_NAME=relay4u_sandbox
DB_USERNAME=postgres
DB_PASSWORD=postgres
